/**
 * Tracking — Fase 2. Sin suscripción WS en estabilización (orderTracking.js usa polling).
 * @param {import('laravel-echo').Echo} _echo
 */
export function registerTrackingChannels(_echo) {
    // noop
}
