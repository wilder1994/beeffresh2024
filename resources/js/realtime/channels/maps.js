/**
 * Mapa operativo — Fase 2. Sin suscripción WS en estabilización (evita eventos huérfanos).
 * @param {import('laravel-echo').Echo} _echo
 */
export function registerMapsChannels(_echo) {
    // operationsMap.js mantiene polling 15s
}
